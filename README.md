# Fun-App
A two-app Android system where FunCenter provides a bound AIDL service for retrieving indexed images and controlling audio playback via MediaPlayer. FunClient binds to the service to request pictures and manage audio (play, pause, resume, stop), supporting concurrent image requests and lifecycle-aware service binding.
